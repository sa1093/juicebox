<p id="notice"><%= notice %></p>
<div class="col-md-8 col-xs-12">
<h1>JUICES</h1><hr>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Capacity</th>
      <th>Price</th>
      <th colspan="3"></th>
    </tr>
   </div>
  </thead>
  
  <tbody>
    <% @juices.each do |juice| %>
      <tr>          
        <td><%= juice.name %></td>
        <td><%= juice.capacity %></td>
        <td><%= juice.price %></td>
        <td><%= link_to 'Show', juice, class: "btn btn-primary font_no_iro" %></td>
        <td><%= link_to 'Edit', edit_juice_path(juice), class: "btn btn-success" %></td>
        <td><%= link_to 'Destroy', juice, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-danger" %></td>
        <td><%= image_tag juice.image.to_s, style: "width: 70px;" %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Juice', new_juice_path, class: "btn btn-info" %>
</div>

<div class="col-md-4 col-xs-12">
  <div class="list-group">
    <a href="#" class="list-group-item active">テキスト</a>
    <a href="#" class="list-group-item">テキスト</a>
    <a href="#" class="list-group-item">テキスト</a>
    <a href="#" class="list-group-item">テキスト</a>
  </div>
</div>